// Nomenclatura de variável em javaScript

const userName = "Lucas Correa"   // Camel

const user_name = "Lucas Correa"  // Snake tudo minusculo separados por _


// Nomenclatura de funções

function SomaNumeros(a,b) {       // Pascal
    return a + b
}

const primeiroNumero = 10
const segundoNumero = 30

console.log(SomaNumeros(primeiroNumero,segundoNumero))
